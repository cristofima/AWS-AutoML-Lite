# AWS AutoML Lite - Backend Environment Variables
# Copy this file to .env and fill in values from: terraform output
#
# Usage (from project root):
#   cp backend/.env.example backend/.env
#   # Edit backend/.env with your values
#   docker-compose up

# AWS Region
AWS_REGION=us-east-1

# S3 Buckets (from terraform output)
S3_BUCKET_DATASETS=automl-lite-dev-datasets-ACCOUNT_ID
S3_BUCKET_MODELS=automl-lite-dev-models-ACCOUNT_ID
S3_BUCKET_REPORTS=automl-lite-dev-reports-ACCOUNT_ID

# DynamoDB Tables (from terraform output)
DYNAMODB_DATASETS_TABLE=automl-lite-dev-datasets
DYNAMODB_JOBS_TABLE=automl-lite-dev-training-jobs

# AWS Batch (from terraform output)
BATCH_JOB_QUEUE=automl-lite-dev-training-queue
BATCH_JOB_DEFINITION=automl-lite-dev-training-job

# Optional: AWS Profile (default: default)
# AWS_PROFILE=default
